#include<bits/stdc++.h>
using namespace std;
/* we have to check the pivot element that is smaller or not if the pivot is smaller it should be its correct place
if the pivot is larger it should be moved away 
larger from the left and smaller form the right we have to check
if they match just swap the both elements*/
void quicksort(){ 

}
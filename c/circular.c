#include<stdio.h>
#include<stdlib.h>
struct node{
    int data;
    strcut node*next;
};

struct Node *head=NULL;
// Creat new node
struct Node* createNode(int value){
    struct Node* newNode=CreateNode(value);

    if(head==NULL){ //empty
        head=newNode;
        newNode->next=head;
        //inset at beginning 
        void insertBegin()









    }
}


